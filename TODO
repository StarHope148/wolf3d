### TODO ###

- clean the code and norm everything
- check /dev/null and /dev/random files
- check for 641 allocations leaks and 131049 bytes (jcanteau got only 38 allocations leaks on VM)
- SDL_LoadBMP() might not be authorized by the rules --> need to think about it
_______________________________________________________

### DONE ###


- strafing controls not working
- fix controls responsiveness
- fix render and minimap movememt/rotation relation
- fix fisheye effect
- added simple collision detection
- segfault if minimaps drawing exceeds WIDTH or HEIGHT of window (trying to access pixel offscreen)
- add texture loader
- then texture the world
- map format testing
- set spawn point dynamically
- check leaks when exiting :
	__attribute__((destructor)) void no_end(void);
	void    no_end(void)
	{
		while (1);
	}
- need to compile the SDL during/before "make" command
	for example adding "sudo apt-get install libsdl2-dev" in Makefile
_______________________________________________________

### DISCARDED ###

- add smooth sliding while colliding on walls depending of angle